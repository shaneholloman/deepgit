# DeepGit v4 runtime dependencies.
# Prefer: pip install -e ".[semantic]"  (uses pyproject.toml)
dspy>=2.5
deepagents>=0.0.2
langgraph>=0.2.60
langchain-core>=0.3
langchain-groq>=0.2
langchain-google-vertexai>=2.0
httpx>=0.27
anyio>=4.0
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0
numpy>=1.25
mcp>=1.2,<2

# Optional persistent semantic recall (or: pip install "deepgit[semantic]")
# lancedb>=0.5
# fastembed>=0.3
# truststore>=0.8
